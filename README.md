# Arcane-Helper
An Arcane Odyssey tool that helps things in general

Make sure you run pip install -r requirements.txt In the terminal of the folder (Opened by clicking on the folders path and typing cmd) 
This makes the file work because it was written in Python and you need the imports
